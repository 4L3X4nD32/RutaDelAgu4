import { useEffect, useState } from "react";

export default function RutaDelAguaDetail(props) {
    const [RutaDelAgua, setRutaDelAgua] = useState({});

    useEffect(() => {
        setRutaDelAgua({
brand: props.brand,
stock: props.stock
        });
    },[]);

    const handelClick = () => {
        setRutaDelAgua((previousValue) => {
            return ({
                
            }
        }
    }
}
